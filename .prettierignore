# Sorties de build : régénérées à chaque `npm run build`, jamais relues.
#
# Ces lignes ne changent rien tant que `requirePragma` est en place — prettier ne touche
# alors à aucun fichier, ignoré ou non. Elles valent pour le jour où on l'enlèverait.
dist/
dist-ext/
node_modules/
coverage/
